import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'hosuseri.mobiDolar',
  appName: 'MobiDolar',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
